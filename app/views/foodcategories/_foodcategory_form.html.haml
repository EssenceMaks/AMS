.bodyams
  .col-xs-1.col-md-1
    .btn_back
    = link_to "Back", root_path, class: "btn btn-default"
  .col-xs-10.col-md-10
    .tit_desc_img_block
      %h1.colorgold НОВАЯ КАТЕГОРИЯ ДЛЯ ПИЩЕВОЙ ПРОМЫШЛЕННОСТИ
      = simple_form_for @foodcategory, html: { multipart: true } do |f|
        - if @foodcategory.errors.any?
          #errors
            %p
              = @foodcategory.errors.count
              Prevented this foodcategory froms saving
            %ul
              - @foodcategory.errors.full_messages.each do |msg|
                %li= msg
        .panel-body
          = f.input :categoryname, label: 'Имя Food Категории', input_html: { title: "ИМЯ КАТЕГОРИИ", class: 'form-control'}
          
        .sub_button
          = f.button :submit, class: "btn btn-primary sub_button"
        .btn_bottom_back
          = link_to "Back", root_path, class: "btn btn-default btn_bottom_back"
  .col-xs-1.col-md-1